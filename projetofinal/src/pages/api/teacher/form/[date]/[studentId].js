// falta implementar com lógica correta

//retorna formulário com base na data e no id do estudante
app.get("/api/manager/form/:date/:studentId", async (req, res) => {
    try {

    } catch (err) {
        console.log(err)
    }
})


//atualizar o formulário com base na data e no id do estudante


app.patch("/api/manager/form/:date/:studentId", async (req, res) => {
    try {

    } catch (err) {
        console.log(err)
    }
})